# Свет макета «Живая вода»

Вся подсветка макета: дома, фонари, светофоры, «машинки» (машинка с горящими фарами),
плееры. **92 модуля LD58A08** по 8 каналов = **736 каналов**, за **38 шлюзами Ebyte**
(`192.168.69.X:8886`, Modbus RTU-over-TCP). Собирает всё контроллер **«Свет»**
(`192.168.69.106`) — тот же, на котором живёт [`ear/`](../ear/).

Цель: отдать каналы в MQTT, показать их в вебе и уметь включать группами.

## Состояние

Конфиг `wb-mqtt-serial` сгенерирован и проверен вживую: поднялись все 92 устройства,
1080 контролов, ошибок опроса нет, самопроверка адресов 92/92. Дашборды в вебе
установлены.

**Опрос включён** (11.08.2026): 92 устройства отвечают, ошибок нет, значения и
подписи идут в MQTT.

## Адресация

| Что | Как |
|---|---|
| Шлюз | `192.168.69.<последний октет>`, порт `8886`, в конфиге `port_type: tcp` |
| Модуль | Modbus slave id из таблицы, 120…224 |
| Канал | 1…8 внутри модуля |

Топик — техническая адресация, подпись — человеческая:

```
/devices/ld58_<шлюз>_<slave>/controls/<канал>        /devices/ld58_23_193/controls/1
                                      <канал>_pwm    ШИМ, %      (только каналы 7–8)
                                      <канал>_freq   частота, Гц (только каналы 7–8)
                                      addr           адрес модуля, только чтение
```

**Подпись канала — `шлюз|модуль|канал|имя`**, например `23|193|1|Аэродром взлёт,
приземления`. Адрес виден прямо в подписи: строка дашборда однозначно ложится на
топик, а канал ищется в таблице по любому из трёх чисел.

## Регистры LD58A08

| Что | Адрес |
|---|---|
| Каналы 1–6 вкл/выкл | `0x0080`, биты 0–5 |
| Канал 7: частота / ШИМ % / вкл | `0x0060` / `0x0061` / `0x0062` (вкл = `0xFFFF`) |
| Канал 8: частота / ШИМ % / вкл | `0x0070` / `0x0071` / `0x0072` |
| Адрес модуля (самопроверка) | `0x00FD` |

Каналы 1–6 — «медленный ШИМ» (период / время включения) → режим «Мигающий» из
таблицы; каналы 7–8 — быстрый ШИМ (1–20000 Гц, 0–100 %) → режим «ШИМ».
Читать можно максимум 2 регистра за запрос, отсюда `max_read_registers: 1`.
`FC=4` и `FC=2` модуль не поддерживает.

## Дашборды в вебе

**Дашборд на город**, названный просто городом; внутри виджет на каждый модуль
LD58, строки — каналы:

| Дашборд | Виджетов (модулей) |
|---|---|
| Калязин | 14 |
| Углич | 19 |
| Мышкин | 14 |
| Кашин | 6 |
| Кимры | 17 |
| Дубна | 17 |
| Борок | 5 |

Итого 7 дашбордов, 92 виджета, **766 строк** (736 каналов минус 96 свободных, плюс
строка «ШИМ %» у каналов 7–8). На дашборды не выносятся: свободные каналы, служебный
`addr` и **частота ШИМ** — она задаётся один раз в «Устройствах».
`id` дашборда уходит в URL homeui, поэтому только латиница: `dash_light_uglich`.

**`compact` в виджете обязан быть `false`.** В компактном режиме homeui рисует только
органы управления и **прячет подписи** — дашборд превращается в столбик безымянных
переключателей. При `compact: false` подпись видна в `.deviceCell-name`.

## Развёртывание

Опрос (конфиг драйвера), с моста «Тверской» (`192.168.69.104`):

```bash
scp wb-mqtt-serial.maket.json root@192.168.69.104:/tmp/maket_full.json
ssh root@192.168.69.104 'sh /tmp/включить-опрос.sh /tmp/maket_full.json'
```

Скрипт бэкапит конфиг, вливает 38 портов (чужие порты не трогает, операция
идемпотентная), перезапускает `wb-mqtt-serial` и показывает результат. Обратно:

```bash
ssh root@192.168.69.104 'sh /tmp/выключить-опрос.sh'
```

— возвращает самый ранний бэкап `wb-mqtt-serial.conf.bak-maket-*` (состояние «до
макета»).

Дашборды:

```bash
python3 генератор-дашбордов.py            # → дашборды-свет.json
scp дашборды-свет.json root@192.168.69.106:/tmp/light_dashboards.json
scp влить-дашборды.py  root@192.168.69.106:/tmp/
ssh root@192.168.69.106 'python3 /tmp/влить-дашборды.py && systemctl restart wb-homeui-backend'
```

Вливание идемпотентно: свои `dash_light_*` / `w_light_*` пересоздаются, чужие
дашборды, «Ухо» и `defaultDashboardId` не трогаются, конфиг бэкапится в
`wb-webui.conf.bak-light-<дата>`.

## Гочи

- **5 участков-призраков** (Углич 6, Углич 7, Мышкин 7, Дубна 4, Дубна 13) своих
  модулей не имеют: их 40 строк описывают каналы соседних модулей (LD 172/158/208/211).
  При генерации исключаются, иначе один физический канал получил бы два имени.
  776 строк − 40 = 736 = 92×8.
- **Клапан ≠ модуль.** «Участок» в таблице — кусок макета (клапан). Один канал может
  светить сразу на нескольких клапанах (LD 172 канал 5 — на четырёх). Адрес канала —
  это (шлюз, модуль, канал); клапан — атрибут места, их может быть несколько.
- Пустое имя — норма, а не пробел в данных: это «Машинка», по одной на клапан.
- В именах нельзя `$ # + / " '` — схема `wb-mqtt-serial` их не пропустит
  («Failed to validate … pattern»). Внутрисловный слэш («д/с») заменён на `∕` (U+2215).
  Вертикальную черту схема пропускает.
- В таблице были перепутаны шлюзы **.27 и .29** (LD 177–180 на .29, LD 181–182 на .27) —
  нашлось сканированием и сверкой `0x00FD`, исправлено 10.08.2026.
- Краны портовые (LD 139/185, каналы 7–8): в железе 40 Гц / 10–20 %, в таблице
  предупреждение «ШИМ более 30 % не включать».

## Файлы

| Файл | Что |
|---|---|
| `wb-mqtt-serial.maket.json` | конфиг драйвера: 38 портов, 92 устройства, 1080 контролов |
| `дашборды-свет.json` | 7 дашбордов + 92 виджета для `wb-webui.conf` |
| `каналы-топики.csv` | карта «топик ↔ шлюз/модуль/канал»: подписи, назначение, режим, клапаны |
| `адресация.csv` | нормализованная таблица, 776 строк (включая справочные) |
| `генератор-конфига.py` | пересборка конфига и карты топиков из таблицы |
| `генератор-дашбордов.py` | сборка дашбордов из конфига |
| `влить-дашборды.py` | установка дашбордов в `/etc/wb-webui.conf` (выполняется на контроллере) |
| `включить-опрос.sh` / `выключить-опрос.sh` | вкл/выкл опроса макета (выполняются на мосту) |
| `Подключения и адресация_final_03_26.xlsx` | исходная таблица, источник истины |

## Дальше

- **Группы** — включать разом пачку каналов: виртуальными устройствами в `wb-rules`.
  Естественные разрезы: по городу, по клапану, по типу (Дом / Фонарь / Светофор /
  Машинка / Плеер).
- **ШИМ**: частота и скважность лежат в самих регистрах модуля — проверить, переживают
  ли они снятие питания. От этого зависит, надо ли восстанавливать их правилом при старте.
