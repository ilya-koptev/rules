# pump-level

Правило `wb-rules`: насос по уровню воды, датчик 4–20 мА на аналоговом входе.
Рядом [`hmi-panel/`](hmi-panel/) — пульт для Android-панели.

## Поставить

```bash
scp water_level.js root@<контроллер>:/etc/wb-rules/
```
