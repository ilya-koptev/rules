# ear

Правило `wb-rules`: наведение макета РТ-64 по азимуту и элевации, сценарии.

## Поставить

```bash
scp ear.js root@<контроллер>:/etc/wb-rules/
```

## Управление

MQTT: чтение `/devices/ear/controls/<контрол>`, команда — тот же топик с `/on`.
